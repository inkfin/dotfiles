# -*- zsh -*-
# vi: ft=zsh

# ======================================
#  env.zsh
# ======================================
#   Machine-specific environment (create_ — generated once by chezmoi,
#   never automatically updated).
#
#   Managed PATH entries live in custom.zsh (tracked in git).
#   Add per-machine settings here: API keys, WSL detection,
#   local tool paths, etc.
# ======================================
